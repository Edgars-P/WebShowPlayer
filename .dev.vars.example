# Copy to .dev.vars (git-ignored) for `wrangler dev`. In production set the same
# value with: wrangler secret put HOST_KEY
# It must match the "Host key" pasted into the player's remote panel.
HOST_KEY=replace-with-a-long-random-secret
